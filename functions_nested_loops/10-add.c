#include "main.h"
/**
 * add - Adition the a and b.
 * @a: numero 1
 * @b: numero 2
 * Descripcion: adds two integers and returns the result.
 * Return: suma de ambos numeros
 */
int add(int a, int b)
{
	int suma;
	
	suma = a + b;
	return (suma);
}
