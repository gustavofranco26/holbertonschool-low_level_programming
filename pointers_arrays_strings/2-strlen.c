#include "main.h"

/**
 *_strlen - Check a function that returns the length of a string.
 *@s: value character
 *Return: Always Longcade
 */
int _strlen(char *s)
{
	int longcade;

	longcade = 0

		if (*s != '\0')
		{
			s++;
			longcade++;
		}
	return (longcade);
}
