#include "main.h"
/**
 * _abs - The absolute value of an integer.
 * @n: Character printer absolut.
 * Return: Absolute Value.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (n);
}
