#include "main.h"
#include <string.h>

/**
*puts2 - a starting with the first character.
*@str: Crachacter Value
*Return: Always (Sucess)
*/

void puts2(char *str)
{
	int i;

	for (i = 0; str[i] != '\0'; i++)
	{
		if ([i / 2] = str[i])
			_putchar(str[i]);
	}
	_putchar('\n');
}
